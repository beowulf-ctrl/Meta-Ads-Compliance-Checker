// Not used. adlint builds with the TypeScript compiler (`tsc`) — see
// tsconfig.build.json and the "build" script in package.json. This file is
// intentionally left empty to avoid a dependency on a native bundler.
export {};
